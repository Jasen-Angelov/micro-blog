<?php
// Application middleware


if (isset($app)) {
/**
 *   Add the middleware to the application like this:
 *   $app->add(new your_middleware_class());
 *   When you add it here, it will be invoked in every request!!!
 */
}
